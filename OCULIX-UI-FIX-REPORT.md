# Oculix UI Fix Report

## المشكلات المعالجة

تم تصغير شريط الزمن على الهاتف ورفعه قليلاً عبر قاعدة mobile-only، مع تقليل ارتفاع شريط الإدخال وحجم زر «الآن» حتى لا يستهلك مساحة كبيرة من المستطيل العلوي.

تم توسيع الترجمة الفعلية للغات الإسبانية والفرنسية والألمانية والتركية. لم تعد هذه اللغات مجرد أسماء في قائمة الإعدادات؛ أصبحت عناوين Settings، أوصافها، أسماء الثيمات والخلفيات، أسماء أقسام مساحة العمل، زر PWA، وشريط Intelligence Lens مترجمة مباشرة. كما توسع قاموس LocaleSurface لعبارات الواجهة الثابتة الشائعة. لا تتم ترجمة أسماء الكيانات أو الأخبار الخارجية أو payloads الحية آلياً.

تم إصلاح الخلفيات عبر ربط الاختيار بعنصر التطبيق المرئي وطبقة CSS مستقلة فوق الخريطة مع opacity وpointer-events:none. السبب السابق أن class الخلفية كان يطبق على body، بينما كانت حاوية main ثابتة بخلفية `var(--bg-void)` وتغطيه.

تمت إضافة زر «تثبيت» ظاهر دائماً في الصفحة. عندما يرسل المتصفح `beforeinstallprompt` يستدعي التثبيت المباشر، وعندما لا يدعم المتصفح الحدث يفتح الزر إرشادات «إضافة إلى الشاشة الرئيسية» أو «تثبيت التطبيق» بدلاً من الاختفاء. الزر مترجم للغات الستة، وتستمر خدمة PWA وmanifest كما هي.

## التحقق

| الفحص | النتيجة |
|---|---|
| Production build | ناجح؛ TypeScript ناجح؛ 56 صفحة ثابتة و69 مسار API |
| Vitest الكامل | 30 ملفاً، 387 اختباراً ناجحاً، 6 متخطاة من أصل 393 |
| الفحص الحي السابق | زر تثبيت ظهر، وشريط الزمن ظهر بتسمية عربية، وشبكة اللغات فتحت بنجاح |
| فحص اللغة الحي | اختيار Español أظهر Settings والثيمات والخلفيات وأقسام workspace وشريط العدسة بالإسبانية، وزر PWA أصبح Instalar |
| HTTP smoke للإصدار الأخير | HTTP 200، حجم HTML 74217 بايت، ووجود Oculix وmanifest وoculix-icon وزر تثبيت |
| الفحص البصري الأخير | أداة المتصفح لم تكن متاحة في هذه الجولة؛ لم يتم الادعاء بفحص بصري جديد بعد آخر build |

## ملاحظة عن الخلفيات

الخلفيات CSS وليست صوراً خارجية ثقيلة. عند اختيار خلفية، يحفظ التطبيق الاختيار في `localStorage`، يضع `background-{id}` على main وطبقة الخلفية، ويعيد تطبيقه عند تحميل الصفحة. تأثير الخريطة نفسها قد يقلل التباين البصري للطبقة، لذلك استخدمت طبقة overlay شفافة بدلاً من الاعتماد على body وحده.
