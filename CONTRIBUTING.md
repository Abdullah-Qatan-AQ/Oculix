# Contributing to Oculix

شكراً لمساهمتك في Oculix. حافظ على المبدأ الأساسي للمشروع: إضافة قدرات تحليلية أقوى دون إخفاء أو كسر الخريطة واللوحات الأصلية.

## قبل فتح Pull Request

شغّل `npm install --no-audit --no-fund` ثم `npm test -- --run` و`npm run build`. إذا غيّر الطلب واجهة أو اتجاه RTL، اختبر العربية وEnglish على سطح المكتب والهاتف. وإذا لمس RECON أو أي route يأخذ هدفاً من المستخدم، أضف اختباراً لحظر العناوين الخاصة والمهل والحدود وعدم تسريب الأسرار.

## قواعد البيانات الحية

لا تضع payloads أو مفاتيح API أو بيانات شخصية حقيقية في الاختبارات. استخدم fixtures صغيرة واضحة، واحفظ source وtimestamp وfreshness عندما تضيف نوعاً جديداً للبيانات. لا تترجم أسماء الكيانات الحية أو IP أو URL أو CVE آلياً.

## واجهة المستخدم

أبقِ أدوات الخريطة الأصلية مرئية وقابلة للوصول. أي لوحة جديدة يجب أن تملك زر إغلاق، وحالة فارغة، ومساراً واضحاً للعودة إلى الخريطة. استخدم `dir="rtl"` للنص العربي و`dir="ltr"` للعلامات والعناوين التقنية المختلطة، واحترم `prefers-reduced-motion`.

## الأمن

راجع `SECURITY.md` قبل تعديل RECON أو proxy routes. لا تستخدم redirect تلقائياً في طلبات scanner، ولا تضف endpoint ينفذ طلباً إلى هدف المستخدم دون تمريره عبر حارس SSRF وتحديد البروتوكول والمهلة والحجم.

## النطاق

الميزات التي تحتاج Redis أو queue أو worker دائم أو خدمة خارجية يجب أن تأتي مع وصف architecture واضح وخيار fallback محلي. لا تُضاف أسرار أو خدمات مدفوعة إلى CI، ولا يتم اعتماد تشغيل دائم قبل اختيار بيئة الاستضافة المناسبة.
