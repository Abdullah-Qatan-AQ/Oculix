# Video Error Findings

مصدر الفحص: تسجيل الشاشة المحمول المرفق `Screen_Recording_٢٠٢٦٠٨٢٧_٠٣١٨٠٢_Chrome.mp4`.

| التوقيت | الملاحظة المؤكدة | موضع الإصلاح |
|---|---|---|
| 00:00–00:04 | عنوان الصفحة طويل ومقطوع على الهاتف (`OCULIX — Op...`). | `src/app/layout.tsx` metadata title/template. |
| طوال الفيديو | كثافة الأزرار والنصوص عالية على الهاتف، وبعض أهداف اللمس صغيرة. | قواعد mobile في `globals.css` وCSS الألواح الجديدة، دون تغيير أدوات الخريطة. |
| 00:05–00:08 | Command Palette عربية، لكن اختصارات `1x/2x` تأتي في الجهة غير المتوازنة داخل RTL. | `CommandPalette.tsx` وCSS direction/shortcut placement. |
| 00:17–00:18 | Source Health يعرض kicker إنجليزياً داخل عنوان عربي، وفراغاً كبيراً في حالة عدم وجود مصادر، والنص التحذيري صغير. | `SourceHealthPanel.tsx` وmobile CSS. |
| 00:20–00:25 | Analyst Mode مزدحم على الهاتف، وكلمة Snapshot غير معرّبة بالكامل. | `AnalystWorkspace.tsx` وmobile CSS/copy. |
| 00:31–00:36 | أخبار بلغات حية مختلفة، لكن صياغة العمر مثل `3h ago` إنجليزية داخل الواجهة العربية. | لا تُترجم payloads الحية؛ تُضاف formatter/واجهة عمر عربية آمنة في `IntelFeed` إن كان النص مولداً محلياً. |
| 00:38–00:39 | اختصارات `NUC`, `WX`, `CAM`, `SAT`, `AIR` غير مشروحة في الهاتف العربي. | labels/ARIA في `page.tsx` وLayer/Stats مع إبقاء الاختصارات التقنية. |
| 00:42–00:48 | أسماء الثيمات التقنية تبقى إنجليزية، ودليل الاستخدام يحتاج حالة فتح واضحة. | `SettingsPanel.tsx` وCSS/labels. |
| 00:57–01:00 | Recon يمزج `SELF TRACK`, `GLOBAL SWEEP`, و`PORT فحص` بطريقة غير مريحة. | قاموس/labels في `ReconPanel.tsx` أو `OsintPanel.tsx` مع الحفاظ على كل أدوات الفحص. |
| 00:09–00:16 | تقطيع خفيف أثناء تدوير الكرة الأرضية محتمل على الهاتف. | تقليل مؤثرات overlay غير الضرورية على mobile واحترام reduced motion؛ لا نغير MapLibre data flow دون قياس. |
| طوال الفيديو | العنوان الظاهر `SOURCE HEALTH` و`ANALYST MODE` و`OCULIX RECON` يحتاج تعريب kicker مع إبقاء العلامة التقنية LTR. | localized kicker strings مع `dir="ltr"` للعلامات. |

## حدود الملاحظة

الفيديو يثبت مشاكل العرض المذكورة، لكنه لا يثبت وحده أن كل زر لا يعمل أو أن مزود البيانات فاشل. لذلك ستُصلح النصوص والتخطيط المؤكدان، وتُختبر endpoints والبناء، ولا تُدّعى سلامة البث الخارجي إذا كان المزود نفسه يمنع التشغيل.

## Live browser verification after fixes

في `http://localhost:3004` ظهر عنوان الصفحة المختصر `Oculix — Live Intelligence`، وظهرت الخريطة والعدادات والـFocus Modes بالعربية، كما ظهرت الإعدادات ودليل الاستخدام بالعربية مع الثيمات الجديدة. ظهرت أسماء Oculix بصيغة صحيحة غير مقلوبة.

عند فتح الإعدادات ظهرت التسميات العربية مباشرة داخل React: «مركز إعدادات Oculix»، «جوهر Oculix»، «بروتوكول الشبح»، «إشارة الشفق»، «مركز الجمر»، ووحدات دليل الاستخدام العشر. ما زال زر Command palette يعرض الاختصار `⌘K` و`Command palette` في tooltip، وهذا عنصر تقني صغير يمكن تعريبه في إصلاح لاحق. ظهرت روابط Discord وX والتوثيق أسفل الشاشة؛ لا تتضمن دفعاً أو اشتراكاً، ولذلك لم تُحذف ضمن طلب إزالة العناصر المدفوعة، لكن يمكن فصلها أو إخفاؤها من الواجهة إذا اعتمد المستخدم ذلك صراحة.

ظهور أرقام صفراء مرقمة ووسم Next.js Dev Tools في لقطة المتصفح ناتج عن أدوات التطوير في بيئة الفحص، وليس من واجهة Oculix الإنتاجية.
